import React from 'react'

export default function Line({width=100,className=""}) {
  return (
    <div style={{padding:.5,backgroundColor:"gray",width:width}} className={className}>

    </div>
  )
}
